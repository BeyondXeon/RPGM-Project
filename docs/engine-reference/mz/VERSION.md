# RPG Maker MZ — Version Reference

| Field | Value |
|-------|-------|
| **Engine Version** | 1.1.1 (local `newdata` template core, read from `rmmz_core.js`) |
| **Upstream Stable** | ~1.9.x–1.10.0 (web search 2026-09-18; listings partially inconsistent — confirm in editor/Steam) |
| **Project Pinned** | 2026-09-18 |
| **LLM Knowledge Cutoff** | May 2025 |
| **Risk Level** | LOW — MZ core API (rmmz_*.js) stable since 2020, within training data |

## Note

This engine version is within the LLM's training data. Engine reference
docs are optional but can be added later if agents suggest incorrect APIs.

Refresh after creating the MZ game project in the editor (it may deploy a
newer corescript than the 1.1.1 template core): run `/setup-engine refresh`
to re-pin the version at that point.

Run `/setup-engine refresh` to populate full reference docs at any time.
